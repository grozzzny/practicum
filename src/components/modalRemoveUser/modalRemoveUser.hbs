<div>
  {{#if modalVisible}}
    <div class='modal__background'>
      {{#> Modal type="default" title="Remove User" }}
        {{{ FormOneField btnText="Remove" label="Login" validator=validator ref="form" onSend=onSend}}}
      {{/Modal}}
    </div>
  {{/if}}
</div>

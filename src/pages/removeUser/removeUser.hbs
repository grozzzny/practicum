<div class="container-chats">
  {{> Side }}
  <div class='container-chats__content'>
    {{> Chat }}
  </div>
</div>
<div class='modal__background' page="dialog">
  {{#> Modal type="default" title="Remove User" }}
    {{#> FormAuth }}
      {{{ Field type="text" name="login" label="Login" }}}
      <div class='container-modal__buttons'>
        {{{ Button label="Remove" type="primary" page="dialog" }}}
      </div>
    {{/FormAuth}}
  {{/Modal}}
</div>
